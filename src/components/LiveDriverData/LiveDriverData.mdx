import { Primary, Stories, Controls, Meta } from "@storybook/blocks";

import * as LiveDriverData from "../LiveDriverData/LiveDriverData.stories";

<Meta of={LiveDriverData} />
# LiveDriverData 

This is a single field displaying the live stats of a driver in a race

## How to use

<Primary />
## available inputs
<Controls />
<Stories />
